import metrics from './metrics'
import colors from './colors'

export const layoutStyles = {
  paddingTop: metrics.statusBarHeight + metrics.navBarHeight,
  backgroundColor: colors.defaultPageBg,
  flex: 1
}
